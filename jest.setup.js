// Jest setup file for global test configuration

// Set test timeout for property-based tests (they may take longer)
jest.setTimeout(30000)

// Add custom matchers if needed
// expect.extend({
//   // Custom matchers can be added here
// })
